<Window x:Class="eartq.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ayarlar" Height="380" Width="460" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="#252526" Topmost="True">
    
    <Window.Resources>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#333" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Background" Value="#EEE" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="BorderBrush" Value="#007ACC" />
            <Setter Property="BorderThickness" Value="2" />
        </Style>
    </Window.Resources>
    
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Kamera:" Foreground="White" FontWeight="Bold" Margin="0,5" FontSize="14"/>
        <ComboBox x:Name="CmbCameras" Grid.Row="1" Margin="0,0,0,15" Height="30"/>

        <TextBlock Text="Mikrofon:" Grid.Row="2" Foreground="White" FontWeight="Bold" Margin="0,5" FontSize="14"/>
        <ComboBox x:Name="CmbMicrophones" Grid.Row="3" Margin="0,0,0,15" Height="30"/>

        <TextBlock Text="Kayıt Klasörü:" Grid.Row="4" Foreground="White" FontWeight="Bold" Margin="0,5" FontSize="14"/>
        <Grid Grid.Row="5" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtOutputFolder" IsReadOnly="True" Padding="5" Height="30" Background="#333" Foreground="White" BorderBrush="#555" BorderThickness="1"/>
            <Button Content="..." Width="30" Grid.Column="1" Margin="5,0,0,0" Click="BtnBrowse_Click"/>
        </Grid>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,15,0,0">
            <Button Content="İptal" Width="90" Height="35" Margin="0,0,10,0" Click="BtnCancel_Click" Background="#444" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <Button Content="Kaydet" Width="90" Height="35" Click="BtnSave_Click" Background="#007ACC" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
